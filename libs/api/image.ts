// export const getImageDataJson = async (instaId: string) => {
//     const url = `./imageData/${instaId}.json`
//     const json = (await import(url)).default;
//     console.log(json)
//     return json.data;
//     }
//여기 api 파일은 next 번들에 빌드가 되지 않는것같다
